import { ShowpasswordPipe } from './showpassword.pipe';

describe('ShowpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
